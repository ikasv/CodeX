import React, {useEffect} from 'react';

const Page = props => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);
    
    return (
        <main>
            Hello World!
        </main>
    );
};

export default Page;