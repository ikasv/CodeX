	// init
	const [test_image, setTestImage] = useState(null);
  
   // hook
    useEffect(() => {
		console.log("File has been set.")
	},[test_image]);
  
	// Form Data
	
	const formData = new FormData();
	formData.append("name", 'test name');
	formData.append("test_image", test_image);
  
      const response = await fetch(
      "https://test.photostop.in/ayana-backend/api/vendor-registration",
      {
        method: "POST",
        headers: {
          // "Content-Type": "application/json",
        },
        body: formData
       
      }
    );
	
	 <input
                  onChange={(e) => setTestImage(e.target.files[0])}
                  type="file"
                  className="form-control"
                  value ={""}
                  
                />